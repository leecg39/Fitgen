{
	"name": "monorepo",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "concurrently --kill-others --raw --names api,web \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
		"build": "concurrently --raw \"npm run build --prefix apps/web\"",
		"start": "concurrently --kill-others --raw --names api,web \"npm run start --prefix apps/api\" \"npm run start --prefix apps/web\"",
		"lint": "concurrently --raw \"npm run lint --prefix apps/web\""
	},
	"devDependencies": {
		"concurrently": "^9.1.2"
	},
	"workspaces": [
		"apps/*"
	]
}